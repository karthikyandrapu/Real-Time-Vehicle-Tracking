import 'package:flutter/material.dart';

class Kishore extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Selected')),
      body: Center(
        child: Text('Kishore Page Content'),
      ),
    );
  }
}
